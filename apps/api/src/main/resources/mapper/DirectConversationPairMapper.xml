<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.quochuy.chat.conversation.mapper.DirectConversationPairMapper">

    <select id="findConversationIdByPair" resultType="java.util.UUID">
        SELECT conversation_id
        FROM direct_conversation_pairs
        WHERE user_1_id = #{user1Id}
          AND user_2_id = #{user2Id}
            LIMIT 1
    </select>

    <insert id="insertPair">
        INSERT INTO direct_conversation_pairs (
            conversation_id,
            user_1_id,
            user_2_id
        ) VALUES (
                     #{conversationId},
                     #{user1Id},
                     #{user2Id}
                 )
    </insert>

    <delete id="deleteByConversationId">
        DELETE FROM direct_conversation_pairs
        WHERE conversation_id = #{conversationId}
    </delete>

</mapper>